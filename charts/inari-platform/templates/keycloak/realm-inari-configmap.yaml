{{- if .Values.keycloak.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: inari-realm-inari
  labels:
    {{- include "inari-platform.componentLabels" (dict "root" . "component" "keycloak") | nindent 4 }}
data:
  realm-inari.json: |
    {{- .Files.Get "files/realm-inari.json" | nindent 4 }}
{{- end }}
