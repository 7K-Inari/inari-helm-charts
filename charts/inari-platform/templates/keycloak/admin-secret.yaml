{{- if .Values.keycloak.enabled }}
# Credentials for the confidential admin client (service account) used by
# inari-server / inari-operator to call the Keycloak Admin REST API via the
# client_credentials grant.
apiVersion: v1
kind: Secret
metadata:
  name: inari-keycloak-admin
  labels:
    {{- include "inari-platform.componentLabels" (dict "root" . "component" "keycloak") | nindent 4 }}
type: Opaque
stringData:
  client-id: {{ .Values.keycloak.adminClient.clientId | quote }}
  client-secret: {{ .Values.keycloak.adminClient.secret | quote }}
{{- end }}
