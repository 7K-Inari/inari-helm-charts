{{- if and .Values.inariServer.enabled .Values.inariServer.route.enabled }}
{{- $gateway := .Values.gateway }}
{{- if not $gateway.name }}
{{- fail "gateway.name is required when a route is enabled — the chart does not create a Gateway; point gateway.name at the Gateway you have already deployed" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "inari-platform.fullname" . }}-server
  {{- with .Values.inariServer.route.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "inari-platform.componentLabels" (dict "root" . "component" "inari-server") | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gateway.name }}
      {{- with $gateway.namespace }}
      namespace: {{ . }}
      {{- end }}
      {{- with $gateway.sectionName }}
      sectionName: {{ . }}
      {{- end }}
  {{- with .Values.inariServer.route.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    - matches:
        {{- with .Values.inariServer.route.pathPrefix }}
        - path:
            type: PathPrefix
            value: {{ . | quote }}
        {{- else }}
        []
        {{- end }}
      backendRefs:
        - name: {{ include "inari-platform.fullname" . }}-server
          port: {{ .Values.inariServer.service.port }}
{{- end }}
