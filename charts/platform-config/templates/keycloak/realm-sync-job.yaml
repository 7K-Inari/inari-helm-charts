{{- if .Values.keycloak.realmSync.enabled }}
# Idempotent realm sync (keycloak-config-cli) running on EVERY install/upgrade.
# Unlike the one-shot KeycloakRealmImport, this applies realm changes (clients,
# scopes, mappers, users) to an existing realm — realm drift across chart
# upgrades is the gap this closes.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "platform-config.fullname" . }}-realm-sync
  labels:
    {{- include "platform-config.componentLabels" (dict "root" . "component" "keycloak") | nindent 4 }}
data:
  realm-inari.json: |
    {{- tpl (.Files.Get "files/realm-inari.json") . | fromJson | toYaml | nindent 4 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "platform-config.fullname" . }}-realm-sync
  labels:
    {{- include "platform-config.componentLabels" (dict "root" . "component" "keycloak") | nindent 4 }}
  annotations:
    "argocd.argoproj.io/hook": PostSync
    "argocd.argoproj.io/hook-delete-policy": BeforeHookCreation,HookSucceeded
    "argocd.argoproj.io/hook-weight": "7"
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: keycloak-config-cli
          image: {{ .Values.keycloak.realmSync.image | quote }}
          env:
            - name: KEYCLOAK_URL
              value: {{ include "platform-config.keycloakBaseUrl" . | quote }}
            # The admin client lives in the "inari" realm; config-cli logs in
            # with client credentials instead of an admin user/password.
            - name: KEYCLOAK_REALM
              value: inari
            - name: KEYCLOAK_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "platform-config.keycloakAdminSecretName" . }}
                  key: {{ include "platform-config.keycloakAdminClientIdKey" . }}
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "platform-config.keycloakAdminSecretName" . }}
                  key: {{ include "platform-config.keycloakAdminClientSecretKey" . }}
            - name: IMPORT_FILES
              value: /config/realm-inari.json
            - name: IMPORT_REPLACE-PLACEHOLDERS
              value: "false"
          volumeMounts:
            - name: realm
              mountPath: /config
      volumes:
        - name: realm
          configMap:
            name: {{ include "platform-config.fullname" . }}-realm-sync
{{- end }}
