# chart-testing configuration
target-branch: main
chart-dirs:
  - charts
chart-repos:
  - cloudnative-pg=https://cloudnative-pg.github.io/charts
  - nats=https://nats-io.github.io/k8s/helm/charts/
  - openfga=https://openfga.github.io/helm-charts
# Version bumps are owned by release-please; ct's increment check would
# block annotation-only Chart.yaml changes and duplicate the Release PR.
check-version-increment: false
helm-extra-args: --timeout 600s
debug: true
